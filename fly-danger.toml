app = 'zen-demo-ruby-danger'
primary_region = 'ams'
console_command = '/rails/bin/rails console'

[build]

[env]
  AIKIDO_DISABLE = 'true'
  AIKIDO_CLIENT_IP_HEADER = 'HTTP_FLY_CLIENT_IP'

[processes]
  app = './bin/rails server'

[[vm]]
  memory = '2048mb'
  cpu_kind = 'performance'
  cpus = 1
